package com.rawsmusic.core.ui.widget.player

/**
 * Shared artwork shape endpoints used by both the real pages and their transition overlay.
 *
 * Keep these values in one place so the overlay cannot settle to a different corner radius
 * from the page that takes over after the transition.
 */
internal const val STANDARD_PLAYER_ARTWORK_CORNER_RADIUS_DP = 28f
internal const val LYRIC_HEADER_ARTWORK_CORNER_RADIUS_DP = 24f
